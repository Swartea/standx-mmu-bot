CHAIN=bsc
WALLET_PRIVATE_KEY=REPLACE_ME

SYMBOL=BTC-USD

# 关闭 USD 模式（走 BPS uptime）
ABS_SPREAD_USD=0

# qty (per order)
QTY=0.0001

# bps params (keep within 10 bps)
TARGET_BPS=9.6
MAX_BPS=9.95
MIN_BPS=0.5

# ladder
LADDER_LEVELS=2
LADDER_STEP_BPS=0.25

# loop
LOOP_MS=5000
MIN_REFRESH_MS=45000

# order check
ORDER_CHECK_MS=5000
STOP_ON_FILL=0

TIME_IN_FORCE=alo

# optional: qty jitter (risk smoothing)
QTY_JITTER_PCT=0.15
QTY_MIN=0.0001
QTY_MAX=0.00013
